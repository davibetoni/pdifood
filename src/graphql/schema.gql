scalar Datetime

type Restaurant {
  id: String
  name: String
  created_at: Datetime
  updated_at: Datetime
}

type Mutation {
  createRestaurant(name: String!): Restaurant
}

type Query {
  restaurants(id: String): [Restaurant]!
}
